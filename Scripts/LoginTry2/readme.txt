You won't be able to run this here as there is no access to a database connection.

You will need to have a database running with a users table.

TABLE:
users

COLUMNS:
id int(5) unsigned primary key
username varchar(100)
password varchar(100)

At the very least.